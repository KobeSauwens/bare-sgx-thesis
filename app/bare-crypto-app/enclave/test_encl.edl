enclave {
	trusted {
        include "test_encl.h"
        /* define ECALLs here. */
        
        //#define ENCL_OP_MATH_SIZE_LEN 32  // 256 bits
        public void do_encl_op_hmac([in, out] struct encl_op_hmac * op);
        
        
        };
	
	//untrusted {
        /* define OCALLs here. */
	//};
};